console.log("----ternary operator example---");
let num=8;
let result= num%2;
result= result==0?"even no": "odd no" // terneary opeartor output will be assigne to the rsult variable
console.log(result);
//example or template litrals where we can print atual value of the variable in string.
console.log(`the ${num} is a ${result}`);
console.log(`my name is
ram ranjan ojha`);